services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=dev-secret-key
      - DATABASE_URL=sqlite:////app/data/optimum.db
    volumes:
      - db-data:/app/data

volumes:
  db-data:
